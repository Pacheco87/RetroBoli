{
  "name": "retroboli",
  "version": "0.1.0",
  "private": true,
  "description": "Escaparate de productos retro de segunda mano con compra externa en Wallapop.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "validate": "npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
